[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "ecommers-flask-app"
version = "1.0.0"
description = "Ecommers Flask Application"
requires-python = ">=3.12"